spring:
  application:
    name: restaurante
  datasource:
    url: jdbc:postgresql://localhost:5432/restaurante_DB
    username: postgres
    password: 224466
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect